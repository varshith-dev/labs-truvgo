import type { KeyboardEvent } from "react";
export interface UseListNavigationOptions {
    isOpen: boolean;
    setIsOpen: (open: boolean) => void;
    itemCount: number;
    scrollToIndex?: (index: number) => void;
}
/**
 * Shared keyboard navigation hook for list-like popups (Select, Dropdown).
 * Handles Arrow up/down, Home, End, Escape, and focus management.
 */
export declare function useListNavigation<TriggerElement extends HTMLElement = HTMLButtonElement>({ isOpen, setIsOpen, itemCount, scrollToIndex, }: UseListNavigationOptions): {
    activeIndex: number | null;
    setActiveIndex: import("react").Dispatch<import("react").SetStateAction<number | null>>;
    itemRefs: import("react").RefObject<(HTMLButtonElement | null)[]>;
    triggerRef: import("react").RefObject<TriggerElement | null>;
    handleTriggerKeyDown: (e: KeyboardEvent) => void;
    handleListKeyDown: (e: KeyboardEvent) => void;
};
