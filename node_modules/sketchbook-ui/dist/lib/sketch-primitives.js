import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { useRef, useState, useEffect } from "react";
export const NOISE_ID = "sk-n";
export const NOISE_FINE_ID = "sk-nf";
export const DISP_AVATAR_ID = "sk-da";
export const DISP_BADGE_ID = "sk-db";
export const DISP_DIVIDER_ID = "sk-dd";
export const DISP_SLIDER_ID = "sk-ds";
export const DISP_PROGRESS_ID = "sk-dp";
/** Place once at app root. Provides shared SVG noise filters and patterns. */
export function SketchProvider({ children }) {
    return (_jsxs(_Fragment, { children: [_jsx("svg", { width: "0", height: "0", style: { position: "absolute", pointerEvents: "none" }, "aria-hidden": "true", children: _jsxs("defs", { children: [_jsxs("filter", { id: `${NOISE_ID}-n`, children: [_jsx("feTurbulence", { type: "fractalNoise", baseFrequency: 0.9, numOctaves: 4, stitchTiles: "stitch" }), _jsx("feColorMatrix", { type: "saturate", values: "0" })] }), _jsx("pattern", { id: `${NOISE_ID}-np`, x: "0", y: "0", width: "100", height: "100", patternUnits: "userSpaceOnUse", children: _jsx("rect", { width: "100", height: "100", filter: `url(#${NOISE_ID}-n)`, opacity: "0.4" }) }), _jsxs("filter", { id: `${NOISE_FINE_ID}-n`, children: [_jsx("feTurbulence", { type: "fractalNoise", baseFrequency: 0.85, numOctaves: 4, stitchTiles: "stitch" }), _jsx("feColorMatrix", { type: "saturate", values: "0" })] }), _jsx("pattern", { id: `${NOISE_FINE_ID}-np`, x: "0", y: "0", width: "100", height: "100", patternUnits: "userSpaceOnUse", children: _jsx("rect", { width: "100", height: "100", filter: `url(#${NOISE_FINE_ID}-n)`, opacity: "0.4" }) }), _jsxs("filter", { id: DISP_AVATAR_ID, children: [_jsx("feTurbulence", { baseFrequency: 0.03, numOctaves: 3, seed: 4, result: "noise" }), _jsx("feDisplacementMap", { in: "SourceGraphic", in2: "noise", scale: 1.8, xChannelSelector: "R", yChannelSelector: "G" })] }), _jsxs("filter", { id: DISP_BADGE_ID, children: [_jsx("feTurbulence", { baseFrequency: 0.03, numOctaves: 2, seed: 2, result: "turbulence" }), _jsx("feDisplacementMap", { in: "SourceGraphic", in2: "turbulence", scale: 1.5 })] }), _jsxs("filter", { id: DISP_DIVIDER_ID, children: [_jsx("feTurbulence", { type: "fractalNoise", baseFrequency: 0.08, numOctaves: 2, result: "noise" }), _jsx("feDisplacementMap", { in: "SourceGraphic", in2: "noise", scale: 2 })] }), _jsxs("filter", { id: DISP_SLIDER_ID, children: [_jsx("feTurbulence", { type: "fractalNoise", baseFrequency: 0.8, numOctaves: 3, result: "noise" }), _jsx("feDisplacementMap", { in: "SourceGraphic", in2: "noise", scale: 2 })] }), _jsxs("filter", { id: DISP_PROGRESS_ID, children: [_jsx("feTurbulence", { type: "fractalNoise", baseFrequency: 0.05, numOctaves: 2, result: "noise" }), _jsx("feDisplacementMap", { in: "SourceGraphic", in2: "noise", scale: 1.5 })] })] }) }), children] }));
}
/** @deprecated Use `SketchProvider` instead. */
export function SketchNoiseDefs({ id, baseFrequency = 0.9 }) {
    return (_jsxs(_Fragment, { children: [_jsxs("filter", { id: `${id}-n`, children: [_jsx("feTurbulence", { type: "fractalNoise", baseFrequency: baseFrequency, numOctaves: 4, stitchTiles: "stitch" }), _jsx("feColorMatrix", { type: "saturate", values: "0" })] }), _jsx("pattern", { id: `${id}-np`, x: "0", y: "0", width: "100", height: "100", patternUnits: "userSpaceOnUse", children: _jsx("rect", { width: "100", height: "100", filter: `url(#${id}-n)`, opacity: "0.4" }) })] }));
}
// eslint-disable-next-line react-refresh/only-export-components
export const noiseUrl = (fine = false) => `url(#${fine ? NOISE_FINE_ID : NOISE_ID}-np)`;
export function SketchPaper({ d, w, h, bg, overlay, overlayOpacity = 0.6, noiseOpacity = 0.12, fine = false }) {
    return (_jsxs(_Fragment, { children: [_jsx("path", { d: d, fill: bg, stroke: "none" }), _jsx("path", { d: d, fill: overlay, opacity: overlayOpacity }), _jsx("rect", { x: "0", y: "0", width: w, height: h, fill: noiseUrl(fine), opacity: noiseOpacity })] }));
}
export function SketchBorder({ d1, d2, stroke, w1 = 2.5, w2 = 1.5, o1 = 0.85, o2 = 0.3, dash = "1,0.5", show = true, style }) {
    if (!show)
        return null;
    return (_jsxs(_Fragment, { children: [_jsx("path", { d: d1, stroke: stroke, strokeWidth: w1, fill: "none", strokeLinecap: "round", strokeLinejoin: "round", opacity: o1, strokeDasharray: dash, style: style }), d2 && (_jsx("path", { d: d2, stroke: stroke, strokeWidth: w2, fill: "none", strokeLinecap: "round", strokeLinejoin: "round", opacity: o2 }))] }));
}
/** Measures element size via ResizeObserver. `w`/`h` never drop below the supplied minimums. */
// eslint-disable-next-line react-refresh/only-export-components
export function useSketchSize(minW = 0, minH = 0) {
    const ref = useRef(null);
    const [size, setSize] = useState({ w: minW, h: minH });
    useEffect(() => {
        const el = ref.current;
        if (!el)
            return;
        const ro = new ResizeObserver(([entry]) => {
            const { width, height } = entry.contentRect;
            setSize(prev => {
                const nw = Math.max(minW, Math.ceil(width));
                const nh = Math.max(minH, Math.ceil(height));
                if (nw === prev.w && nh === prev.h)
                    return prev;
                return { w: nw, h: nh };
            });
        });
        ro.observe(el);
        return () => ro.disconnect();
    }, [minW, minH]);
    return { ref, w: size.w, h: size.h };
}
