export interface UseVirtualizerOptions {
    count: number;
    itemHeight: number;
    containerRef: React.RefObject<HTMLElement | null>;
    overscan?: number;
}
export interface VirtualItem {
    index: number;
    offsetTop: number;
}
export declare function useVirtualizer({ count, itemHeight, containerRef, overscan }: UseVirtualizerOptions): {
    virtualItems: VirtualItem[];
    totalHeight: number;
    scrollToIndex: (index: number) => void;
};
